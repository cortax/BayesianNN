Make/Tests Models objects (Make: TCN, CNN, Dilated CNN, NeuralODEN, MaxoutN) (Verify: FCN, GRU, LSTM)
Remake Experiments files (pipelines)
Remake Main (Redux)

Eval metrics for significative events inside windows
Think about splitting along features instead of flights

Models: Input changes (LogNorm, Embeddings, autoencoding), 
        Output Changes(Better Classif/Regression Head, use RNN outputs instead of H_n),
        Attention, 
        Ensemble